version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.20.2
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 sync build/ s3://guioapp

artifacts:
  files:
    - '**/*'
